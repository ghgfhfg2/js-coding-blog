---
title: 마지막 칸까지 가는 최소 점프 수
slug: minimum-jumps-to-end
track: today
difficulty: medium
topic: greedy
tags:
  - daily
  - medium
  - greedy
  - array
  - jump-game
order: 1000
function_name: solution
time_limit_ms: 200
primaryMethod: greedy-farthest-reach-layer-scan
coreIdea: 현재 점프 횟수로 도달 가능한 구간을 한 레벨처럼 보며 그 안에서 닿을 수 있는 가장 먼 위치를 갱신하고, 구간 끝에 도달할 때마다 점프 수를 늘려 마지막 칸까지의 최소 점프 수를 구한다
gimmick: 각 위치에서 실제로 어느 칸으로 점프할지 선택하지 않아도 현재 구간의 최원거리만 추적하면 되고, 마지막 칸에 이미 도달 가능한 상태에서는 점프 수를 불필요하게 한 번 더 늘리지 않도록 처리해야 한다
starter_code: |
  function solution(nums) {
    return 0;
  }
test_cases:
  - input: [[2, 3, 1, 1, 4]]
    output: 2
  - input: [[2, 3, 0, 1, 4]]
    output: 2
  - input: [[0]]
    output: 0
  - input: [[1, 1, 1, 1]]
    output: 3
---

각 칸에서 앞으로 최대 `nums[i]`칸까지 점프할 수 있는 정수 배열 `nums`가 주어질 때, **첫 번째 칸에서 마지막 칸까지 도달하는 최소 점프 횟수**를 반환하는 `solution` 함수를 작성하세요.

항상 마지막 칸까지 도달할 수 있다고 가정합니다.

## 제한사항
- `1 <= nums.length <= 100000`
- `0 <= nums[i] <= 100000`
- 시작 위치는 인덱스 `0`입니다.
- 항상 마지막 칸까지 도달할 수 있습니다.

## 예시
- 입력: `[2, 3, 1, 1, 4]` → 출력: `2`
- 입력: `[2, 3, 0, 1, 4]` → 출력: `2`
- 입력: `[0]` → 출력: `0`

## 힌트
- 현재 점프 수로 갈 수 있는 구간을 하나의 묶음처럼 생각해 보세요.
- 그 구간 안에서 다음 점프가 닿을 수 있는 가장 먼 위치만 알면 됩니다.
- 구간 끝에 닿았을 때 점프 수를 하나 늘리면 됩니다.

## 해설
이 문제의 핵심은 **BFS 레벨처럼 구간을 확장하는 greedy**입니다.

1. 현재 점프 횟수로 도달 가능한 구간의 오른쪽 끝을 `currentEnd`로 둡니다.
2. 그 구간 안의 각 위치에서 `i + nums[i]`로 다음에 닿을 수 있는 가장 먼 위치 `farthest`를 갱신합니다.
3. 인덱스 `i`가 `currentEnd`에 도달하면, 지금까지 본 위치들로 만들 수 있는 다음 구간이 확정되므로 점프 횟수를 1 늘리고 `currentEnd = farthest`로 갱신합니다.
4. 마지막 칸 직전까지만 보면 최소 점프 횟수를 구할 수 있습니다.

예를 들어 `[2, 3, 1, 1, 4]`에서는:
- 처음 점프로 인덱스 `0`에서 시작해 `1` 또는 `2`까지 갈 수 있습니다.
- 그 구간에서 가장 멀리 뻗는 선택은 인덱스 `1`을 거쳐 마지막 칸 근처까지 가는 경우입니다.
- 따라서 최소 점프 수는 `2`입니다.

각 위치를 한 번씩만 보므로 효율적으로 해결할 수 있습니다.
